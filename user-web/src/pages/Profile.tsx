import { createResource, createSignal } from "solid-js";
import { A } from "@solidjs/router";
import { get, post, put } from "../api/client";
type ProfileData = { email: string; displayName?: string; role: string; status: string; emailVerified: boolean; totpEnabled: boolean; createdAt: string };
export default function Profile() { const [profile, { refetch }] = createResource(() => get<ProfileData>("/user/profile")); const [name, setName] = createSignal(""); const [current, setCurrent] = createSignal(""); const [next, setNext] = createSignal(""); const [message, setMessage] = createSignal(""); const save = async (event: Event) => { event.preventDefault(); await put("/user/profile", { displayName: name() }); setMessage("Profile updated"); refetch(); }; const password = async (event: Event) => { event.preventDefault(); await post("/user/password", { currentPassword: current(), newPassword: next() }); setCurrent(""); setNext(""); setMessage("Password changed and other sessions revoked"); }; return <div class="space-y-8"><div><p class="eyebrow">Identity</p><h1 class="title">Profile</h1><p class="muted">Manage the account attached to your API access.</p></div>{message() && <p class="notice">{message()}</p>}<section class="panel"><div class="section-head"><h2>Account</h2></div><div class="grid gap-4 sm:grid-cols-2"><div><p class="metric-label">Email</p><p class="mt-1 font-medium">{profile()?.email}</p><p class="mt-1 text-sm">{profile()?.emailVerified ? <span class="text-emerald-700">Verified</span> : <A href="/verify-email" class="text-slate-700 underline">Verify email</A>}</p></div><div><p class="metric-label">Status</p><p class="mt-1 font-medium">{profile()?.status}</p></div><div><p class="metric-label">Security</p><p class="mt-1 font-medium">{profile()?.totpEnabled ? "Authenticator enabled" : "Password only"}</p></div><div><p class="metric-label">Joined</p><p class="mt-1 font-medium">{profile()?.createdAt && new Date(profile()!.createdAt).toLocaleDateString()}</p></div></div><form class="mt-6 flex max-w-lg gap-3" onSubmit={save}><input class="field" placeholder={profile()?.displayName || "Display name"} value={name()} onInput={e => setName(e.currentTarget.value)} /><button class="primary">Save name</button></form></section><section class="panel"><div class="section-head"><h2>Change password</h2></div><form class="grid max-w-lg gap-3" onSubmit={password}><input required type="password" class="field" placeholder="Current password" value={current()} onInput={e => setCurrent(e.currentTarget.value)} /><input required minlength="12" type="password" class="field" placeholder="New password (12+ characters)" value={next()} onInput={e => setNext(e.currentTarget.value)} /><button class="primary justify-self-start">Change password</button></form></section></div>; }
